spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testgen?useSSL=false&serverTimezone=UTC
    username: testgen
    password: testgen
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    locations: classpath:db/migration
server:
  port: 8080
